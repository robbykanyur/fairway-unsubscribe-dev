version: '2'

services:
  build:
    image: unsubscribe-assets
    ports:
      - "2992:2992"
    build:
      context: ../
      dockerfile: assets/Dockerfile
    volumes:
      - ../src:/home/node/src
    entrypoint: ["./assets.sh"]

